# coAgent V1：目标、阶段与开发清单

> 文档版本：0.1 · 更新日期：2026-09-21  
> 状态：S0 开发中；下方勾选仅代表对应子项有证据，S0 双真实模型关口尚未通过。  
> 对应设计：[项目设计文档](/Users/c4n6r/cypherSec/coAgent/PROJECT_DESIGN.md)

## 1. V1 的一句话目标

交付一个个人使用的桌面工作 Agent：打开应用、配置模型、选择本地目录，即可通过图形界面完成可观察、可停止、可查看成果、可恢复历史的工作任务。

V1 不追求复制完整 Codex 桌面产品，也不追求一次接入所有模型。它验证“独立前端 + Codex 执行层 + 可扩展模型网关”的闭环。

## 2. 发布目标与明确边界

### 2.1 必须交付的目标

| 目标 ID | V1 目标 | 可观察的验收结果 |
| --- | --- | --- |
| G1 | 桌面应用与工作空间 | 在首发系统安装启动，选择并记住工作目录 |
| G2 | 会话生命周期 | 新建、读取、恢复，重启后继续已有会话 |
| G3 | 可观察与可控执行 | 流式文本、工具时间线、必要确认、停止及最终状态 |
| G4 | 多模型能力 | 两个具体服务通过测试，覆盖 Responses 与 Chat Completions 两类协议 |
| G5 | 成果与变更 | 文件确实存在，文本预览、Diff、系统打开；支持范围内的文件恢复 |
| G6 | 本地数据可靠性 | 无自建业务数据库；配置异常可恢复；凭据与任务环境分离 |
| G7 | 工作模板 | 至少三个本地 Markdown 模板，可预览和编辑后执行 |
| G8 | 可复现发布 | 版本锁定、关键测试通过、安装和回退步骤可复现 |

### 2.2 V1 不纳入的功能

- 账号、登录、组织、角色、团队权限和多租户。
- 自建 SQLite 业务数据库、远程数据库、Redis 与完整事件仓库。
- 云端控制台、浏览器远程访问、远程执行集群。
- 多 Agent 编排、同时运行多个任务、定时后台任务。
- 完整知识库、向量检索、自动长期记忆。
- 通用 Word / PDF / Excel 编辑器和专用预览。
- 插件商店、完整 MCP 管理界面、企业业务系统连接器。
- 自动更新服务、自动跨模型容错切换。

这些功能属于后续版本，不作为 V1 验收前提。运行时内部已有存储与机制照常保留，不为了简化业务层而删除上游依赖。

### 2.3 V1 固定的简化选择

- macOS 优先，具体 CPU 架构在 S0 冻结。
- 单主窗口、单应用实例、全应用最多一个执行中的任务。
- 应用自有数据使用文件；会话通过 Codex 接口管理。
- 同一轮执行固定模型；跨供应商切换创建新会话。
- 文本工作优先；图片、复杂办公格式不是发布门槛。
- 默认不修改 Codex 内核；若需补丁，先给出复现与最小变更理由。

## 3. 阶段总览

以下为一名熟悉 TypeScript 的全栈开发者的有效开发日估算，不含等待凭据、签名账号或外部服务恢复的时间。

| 阶段 | 内容 | 预计开发日 | 主要目标 |
| --- | --- | --- | --- |
| S0 | 技术验证与版本冻结 | 3–5 | 先验证执行与异构模型可行性 |
| S1 | 工程骨架与基础存储 | 3–4 | G1、G6 基础 |
| S2 | 单模型端到端执行 | 5–7 | G2、G3 |
| S3 | 模型网关与配置界面 | 5–8 | G4 |
| S4 | 工作空间、成果与模板 | 4–6 | G5、G7 |
| S5 | 稳定性与恢复 | 4–6 | G6、回归 |
| S6 | 打包、试用与发布 | 3–5 | G8、全量验收 |

合计约 27–41 个有效开发日；按单人投入及必要缓冲，日历排期建议预留约 7–10 周。该估算以 S0 验证结果为前提，复杂协议缺口需要单独调整。

依赖关系：`S0 → S1 → S2 → S3 → S4 → S5 → S6`。可以交错完善 UI，但不要在 S0 的模型执行验证前投入大规模视觉与扩展功能。

每一阶段都按“触发条件 → 开发内容 → 测试反馈 → 退出条件”推进。未通过关口时，修复对应问题并重测，而不是用 UI 演示代替底层验证。

## 4. S0：技术验证与版本冻结

**进入条件**：设计文档形成基线，并能取得至少两个用于测试的模型服务配置。

### 开发内容

- [ ] S0-01：核查上游开源组件、许可证、二进制来源及分发要求。
- [x] S0-02：选定 Codex tag / commit，记录版本、来源、哈希与目标系统架构。
- [x] S0-03：创建临时测试工作区和独立 CODEX_HOME，不影响用户已有 Codex 数据。
- [x] S0-04：生成对应版本协议类型，完成 stdio 握手、会话创建、启动与停止。
- [x] S0-05：用一个 Responses 兼容服务完成“读取 → 修改 → 测试 → 输出结果”。
- [ ] S0-06：采集脱敏请求与事件样本，确认实际工具格式、上下文及端点需求。
- [ ] S0-07：实现最小 Chat Completions 转换原型，用第二个服务完成同一任务。
- [x] S0-08：验证会话历史读取与重启恢复，不重复建设会话数据库。
- [x] S0-09：登记协议缺口、实验性接口依赖及需要调整的排期。

2026-09-21 进展：固定 Codex 0.155.1；真实运行时配合本地脚本模型完成文件任务、历史恢复与取消。首个真实模型选定 deepseek-flash，充值后真实文件任务、进程重启、会话恢复和续轮任务均通过；第二个真实供应商未指定。完整证据与缺口见 [S0 进展](/Users/c4n6r/cypherSec/CoAgent/docs/S0_STATUS.md)。

### 交付物

版本基线记录、协议类型、最小验证程序、脱敏样本、两个模型的兼容性报告、问题列表。

### 测试与反馈

每个模型至少验证：流式文本、一次工具调用、连续工具调用、工具结果回传、工具失败后的处理、取消。记录请求、结果与退出状态，区分协议通过与任务质量通过。

### 退出条件

两个模型均能完成同一个小型文件任务，且工具实际执行结果与界面／日志记录一致。若第二个模型只通过聊天测试，G4 仍未验证；先定位工具兼容缺口。

## 5. S1：工程骨架与基础存储

**进入条件**：S0 通过，首发系统、运行时版本和首批模型组合已登记。

### 开发内容

- [ ] S1-01：建立 TypeScript 工程、依赖锁文件、格式检查与测试入口。
- [ ] S1-02：搭建 Electron Main、Preload、React Renderer 和 Host 启动结构。
- [ ] S1-03：定义带校验的业务 IPC，不暴露任意 shell 或任意文件读写入口。
- [ ] S1-04：实现单实例和全局单任务约束。
- [ ] S1-05：通过系统 API 定位用户数据目录，建立独立运行时目录映射。
- [ ] S1-06：实现 JSON 配置 Schema、默认值、原子替换、备份和迁移入口。
- [ ] S1-07：实现系统凭据读写及 secretRef；前端不持久化明文 API Key。
- [ ] S1-08：建立导航、目录选择、空会话、设置页的基本界面。

### 交付物

可启动工程、基础 UI、配置与凭据模块、进程生命周期骨架、相关单元测试。

### 测试与反馈

覆盖首次启动、二次启动、配置缺失、配置损坏、目录不可访问、重复启动和凭据设施错误。

### 退出条件

应用可以选择目录并记住设置，重启结果一致；损坏配置保留现场并支持恢复，不能静默丢失已有配置。

## 6. S2：单模型端到端执行

**进入条件**：S1 可启动；S0 的首个模型与协议样本可复用。

### 开发内容

- [ ] S2-01：将验证原型封装成 Runtime Adapter，加入请求关联、超时及退出清理。
- [ ] S2-02：实现会话创建、列表、历史读取和恢复；验证列表过滤不漏掉应用会话。
- [ ] S2-03：实现提交轮次、流式文本、工具开始／完成状态和错误展示。
- [ ] S2-04：规范化事件，按会话、轮次、Item 关联；支持快照与事件去重。
- [ ] S2-05：实现审批卡片，绑定动作与进程实例，处理重复点击和过期响应。
- [ ] S2-06：实现停止按钮与 cancelling 状态，等待运行时结束并显示已产生的修改。
- [ ] S2-07：实现基本执行时间线，处理超长日志和输出渲染。
- [ ] S2-08：实现正常关闭；活动任务退出时确认处理方式，清理受管理的子进程。

### 交付物

首个可操作工作台、会话与执行 UI、Runtime Adapter、状态机和执行闭环测试。

### 测试与反馈

使用确定性模型模拟覆盖事件顺序与错误；使用真实模型执行一个代码任务。UI 重新载入后先读取快照，重复事件不能导致重复聊天消息或重复执行。

### 退出条件

用户完全通过 UI 完成“选目录 → 提交 → 审批 → 查看执行 → 停止或完成 → 重启恢复”，无需手动操作 CLI。

## 7. S3：模型网关与配置界面

**进入条件**：S2 闭环稳定；S0 中的异构模型原型与缺口结论明确。

### 开发内容

- [ ] S3-01：定义 CanonicalRequest / CanonicalEvent 与 Provider Adapter 边界。
- [ ] S3-02：生产化 Responses 路径，加入取消、错误映射和有界超时。
- [ ] S3-03：生产化 Chat Completions 转换，覆盖参数分片、工具标识及结果回传。
- [ ] S3-04：为锁定版本实际出现的自定义工具、上下文请求等补齐已承诺的协议集合。
- [ ] S3-05：提供供应商和模型增删改、地址校验、凭据选择及能力检测界面。
- [ ] S3-06：实现 untested / chat / tool / agent 能力分级及检测报告。
- [ ] S3-07：管理模型别名、上下文预算及模型专属参数，不向所有模型发送相同可选参数。
- [ ] S3-08：实现回环监听与进程调用凭据校验，检查工具子进程环境中的凭据隔离。
- [ ] S3-09：显示可获得的用量；未知值保持未知，估算与供应商返回值分开标注。
- [ ] S3-10：模型切换创建新会话，执行中固定模型，不做隐式跨供应商重试。

### 交付物

正式模型网关、两类协议 Adapter、模型管理 UI、模型能力档案及契约测试集。

### 测试与反馈

至少覆盖十类用例：文本、流式、取消、单工具、多轮工具、多调用关联、错误参数、工具失败、超时／限流、长上下文。并行工具、多模态等仅在声明支持时额外验收。

### 退出条件

至少两个具体 Provider / Model 组合通过 Agent 级核心用例，且覆盖两类协议；添加同协议供应商无需修改 UI 业务代码。不支持的能力有明确提示，不伪装成已通过。

## 8. S4：工作空间、成果与模板

**进入条件**：S3 的模型执行路径通过核心契约测试。

### 开发内容

- [ ] S4-01：完善最近目录、目录切换与会话关联；运行中限制切换执行目录。
- [ ] S4-02：记录可验证的产物引用，不把模型输出的文件名直接当成真实文件。
- [ ] S4-03：实现文本／Markdown 预览、Diff、系统打开与文件导出。
- [ ] S4-04：实现受支持文本文件的任务前基线与可逆变更，不覆盖用户原有修改。
- [ ] S4-05：恢复前核对内容哈希，处理后续编辑冲突、新建与删除文件。
- [ ] S4-06：处理快照大小上限和不支持类型，明确哪些文件具备恢复依据。
- [ ] S4-07：实现本地 Markdown 模板加载、内容预览、编辑后提交。
- [ ] S4-08：提供三个模板：项目概览、小改动并验证、资料汇总。
- [ ] S4-09：实现最小成果元信息文件，不引入重复聊天或工具事件数据库。

### 交付物

成果面板、文本 Diff、受控恢复功能、三个模板、文件可靠性测试。

### 测试与反馈

覆盖原本就有未提交修改的工作区、任务后用户再次编辑、文件被删除、二进制文件、大文件和越界路径。

### 退出条件

三类目标任务均能交付真实文件；支持范围内的变更可以恢复，存在冲突时保留用户内容并明确显示状态。

## 9. S5：稳定性、恢复与回归

**进入条件**：V1 的主要功能均可通过 UI 使用。

### 开发内容

- [ ] S5-01：实现单个活动任务的最小恢复记录，不保存完整 token 事件流。
- [ ] S5-02：Host 重启后核对运行时与工作区状态，区分完成、失败和待人工核对。
- [ ] S5-03：进程退出后失效旧审批，处理未完成请求与 UI 等待状态。
- [ ] S5-04：注入断流、超时、配置损坏、子进程崩溃和磁盘写入失败。
- [ ] S5-05：检查重试预算，不重复提交轮次，不自动重放可能已产生副作用的操作。
- [ ] S5-06：实现诊断日志轮转与脱敏，明确诊断包包含的内容。
- [ ] S5-07：检查事件订阅、监听器和子进程清理，避免应用多次启动关闭后泄漏。
- [ ] S5-08：建立不少于 20 个固定任务组成的回归集。

### 交付物

恢复策略实现、故障注入报告、回归样本与结果、未解决问题清单。

### 测试与反馈

确定性关键路径测试必须全部通过；真实模型评测分别记录两个模型的任务完成率、人工介入次数、耗时及成本。失败按模型质量、适配问题、应用缺陷分类，避免混为一谈。

### 退出条件

没有会导致凭据暴露、用户数据丢失或重复写入的已知发布阻断问题。恢复逻辑对不明确的状态标记待核对，而不是自动认定成功或无副作用地失败。

## 10. S6：打包、试用与发布

**进入条件**：S5 完成，发布阻断问题关闭。

### 开发内容

- [ ] S6-01：打包固定版本运行时，记录来源和校验值，保留必要开源声明。
- [ ] S6-02：完成目标系统安装包与签名方案；在干净系统环境验证启动依赖。
- [ ] S6-03：完善首次使用流程：添加模型 → 能力检测 → 选目录 → 首个任务。
- [ ] S6-04：编写安装、数据位置、模型配置、诊断与手动升级说明。
- [ ] S6-05：在数据副本上测试升级和回退，验证配置及运行时状态兼容性。
- [ ] S6-06：进行至少三个工作日的实际使用，记录并关闭高优先级问题。
- [ ] S6-07：执行完整发布验收，生成版本说明及已知问题清单。

### 交付物

V1 安装包、构建与运行时版本记录、用户说明、测试报告、回退记录及发布说明。

### 退出条件

新安装可以完成首个真实任务；应用不依赖开发机隐式安装的 Codex；重新启动能读取历史；回退流程在副本上有实际验证证据。

## 11. 发布验收矩阵

所有数字均为待实现的验收目标，不是已完成结果。

| 验收 ID | 场景 | 通过标准 | 归属 |
| --- | --- | --- | --- |
| A01 | 首次使用 | 无登录步骤，配置模型和目录后可提交任务 | G1 |
| A02 | 重启恢复 | 完成会话可读取并继续，不另建聊天数据库 | G2 |
| A03 | 流式事件 | 文本、工具与最终状态归属正确，重复事件不重复展示 | G3 |
| A04 | 审批 | 重复响应不重复执行，旧进程审批不作用于新任务 | G3 |
| A05 | 停止 | 区分取消中与已停止，保留已发生动作的记录 | G3 |
| A06 | 多模型 | 两个模型、两类协议完成同一组核心 Agent 用例 | G4 |
| A07 | 能力不足 | 执行前或遇到不支持请求时给出明确状态，不静默丢字段 | G4 |
| A08 | 成果 | 真实文件可打开，Diff 与实际变更一致 | G5 |
| A09 | 文件恢复 | 仅恢复有依据的任务变更，用户后续修改触发冲突处理 | G5 |
| A10 | 配置异常 | 中断写入或损坏配置后可以恢复，旧内容未被静默删除 | G6 |
| A11 | 凭据 | 常规日志、前端持久化和工具环境中无供应商明文密钥 | G6 |
| A12 | 崩溃 | 未知执行结果进入待核对状态，不自动重复写入 | G6 |
| A13 | 模板 | 三个模板可预览、修改并用于完成目标场景 | G7 |
| A14 | 安装与回退 | 干净环境安装通过，数据副本上的回退步骤可复现 | G8 |

真实任务质量的初始目标：两个发布模型各运行同一组不少于 20 个固定任务，分别达到至少 80% 的预定义成果标准。每次执行的工具、输入、人工帮助和模型参数需固定或记录；单轮 20 例仅用于内部试用门槛，不代表统计可靠性保证。

对凭据暴露、数据破坏、重复业务写入等问题采用零容忍发布门槛；它们不以平均完成率抵消。

## 12. 每个任务的完成定义

任务进入“完成”必须同时具备：

1. 可定位的实现及清晰接口。
2. 对应测试或可复现验证步骤。
3. 正常路径与至少一个失败路径的结果。
4. 实际运行结果记录，而不仅是代码存在。
5. 必要的说明更新。
6. 不破坏个人单机、无登录、不另建业务数据库的架构约束。

建议记录格式：任务 ID、实现位置、依赖版本、测试命令、输入、输出摘要、退出码、未解决问题。真实模型测试额外记录供应商、模型 ID 和测试时间，凭据不进入记录。

## 13. 最小风险台账

| 风险 | 早期信号 | 处理方式 | 验证点 |
| --- | --- | --- | --- |
| 上游接口变化 | 生成类型与 Adapter 不匹配 | 固定版本，契约回归后再升级 | S0 / S6 |
| 模型仅能聊天 | 工具定义或回传失败 | 用真实轨迹补齐适配，调整能力等级 | S0 / S3 |
| 文件存储复杂化 | 开始自建事务、索引、并发队列 | 先删减需求，再评估存储方案 | S5 |
| 重试引发重复动作 | 同一轮次或工具动作重复出现 | 关联标识、状态核对和有界重试 | S2 / S5 |
| “停止”被误解为撤销 | 文件已经改动但 UI 显示无事发生 | 明确状态与成果，独立文件恢复入口 | S2 / S4 |
| 模型切换污染历史 | 私有标识或工具状态不兼容 | V1 以新会话切换供应商 | S3 |
| 功能膨胀 | 提前引入账号、集群或插件商店 | 按本文件目标表拒绝纳入 V1 发布门槛 | 全阶段 |

## 14. 第一阶段可复用开发提示词

```text
你正在开发 coAgent 的 S0 技术验证阶段。

先阅读：
/Users/c4n6r/cypherSec/coAgent/PROJECT_DESIGN.md
/Users/c4n6r/cypherSec/coAgent/V1_ROADMAP.md

产品约束：个人单机、无账号登录；业务层不用 SQLite；
会话复用 Codex 持久化；配置用文件；优先外围集成。

本次只完成 S0，不扩展完整前端或团队功能：
1. 检查现有工程与运行环境，选定并记录上游版本和来源。
2. 在独立测试目录与 CODEX_HOME 中验证 App Server。
3. 生成对应版本协议类型，打通会话和执行生命周期。
4. 用一个 Responses 服务及一个 Chat Completions 服务，
   验证读取文件、修改文件、运行测试、工具回传和取消。
5. 凭据通过专用入口取得，不写入源码、提示词或普通日志。
6. 缺少真实服务配置时，先完成可运行的模拟协议测试，
   将真实模型验收明确标为待验证，不能据此宣布 S0 完成。

交付版本基线、最小原型、测试证据和兼容性问题列表。
遇到工具语义不兼容时先复现和定位，再提出最小适配方案。
```

## 15. 进入下一阶段前的判断

- 当前阶段的退出条件是否有真实测试证据？
- 未解决问题是否影响后续架构，而不是只影响界面细节？
- 是否出现了应从 V1 删除的额外功能？
- 数据是否仍只有一个主存储，是否重复保存了运行时已有历史？
- 下一阶段是否有明确交付物，而非笼统的“继续完善”？

最终交付标准是个人用户能稳定完成日常工作，而不是组件数量或功能列表长度。

## 2026-09-21 最新执行调整（覆盖此前阶段关口）

用户已明确暂缓第二个模型服务，优先完整固定 Agent 回归与桌面前端。本轮新增 Electron/React 桌面执行、历史、审批和停止闭环；真实 DeepSeek 固定套件及桌面实测分别留证。第二供应商不再阻挡 S1/S2 开发，但不计为已验收。Host 独立进程、设置页、文件/diff 面板和发布包继续排期。当前状态以 [桌面交付记录](/Users/c4n6r/cypherSec/CoAgent/docs/DESKTOP_V1_STATUS.md) 和 [回归逐项结果](/Users/c4n6r/cypherSec/CoAgent/docs/AGENT_REGRESSION.json) 为准。

## 最新更新：deepseek-chat 与工作文件面板

默认请求模型现为 `deepseek-chat`（供应商实测返回 `deepseek-flash` 别名解析结果）。本轮重新完成 20/20 固定 Agent 回归，并新增目录浏览/只读 UTF-8 预览、边界检查与桌面验收。此前 Flash 记录保留为历史。具体以 [本轮验证](/Users/c4n6r/cypherSec/CoAgent/docs/DEEPSEEK_CHAT_VALIDATION.md) 为准；diff 面板与独立 Host 仍待开发。

## 最新更新：启用 Qwen 官方模型与 Git diff

用户已启用第二供应商 Qwen3.5 Flash，覆盖此前暂缓决策。桌面新增 DeepSeek / Qwen 模型选择与配置保存；两者使用官方 OpenAI Responses 兼容入口。新增只读 Git 已暂存/未暂存 diff 面板。实际回归和启动方法见 [Qwen 与 diff 交付记录](/Users/c4n6r/cypherSec/CoAgent/docs/QWEN_AND_DIFF.md)。独立 Host Utility Process 和签名发布仍待开发。

## 最新更新：独立 Host 已落地

Codex 编排、模型网关、配置和文件操作已迁至 Electron Utility Process；Main 保留窗口、原生对话框与受限转发。已新增 Host 强制退出、子进程清理、重新连接和不重放验证。详见 [Host 交付记录](/Users/c4n6r/cypherSec/CoAgent/docs/HOST_PROCESS.md)。活动任务持久化恢复与配置原子写入尚待开发。

## 最新更新：配置可靠性与持久化恢复

已加入配置 Schema、串行原子写入、有效备份及损坏提示；任务执行前持久化最小记录，应用重启后保留待核对状态，显式确认前阻止新任务且不自动重放。详见 [恢复与配置记录](/Users/c4n6r/cypherSec/CoAgent/docs/RECOVERY_AND_CONFIG.md)。文件撤销恢复与系统凭据存储仍待开发。

### 系统凭据与模型设置增量

- 已实现：两个官方模型的凭据设置页、safeStorage 本机密文、保存/替换/删除、来源提示、显式最小连接测试、空闲 Host 同步、损坏修复。
- 本轮验证：凭据单元测试与实际 Electron 保存/重启/删除/修复闭环；其余回归详见 VERIFICATION.txt。
- 待进行：自定义模型/供应商设置、正式安装包签名及升级凭据回归。任意模型支持仍需逐协议能力验证。

### 自定义服务增量

- 已实现：最多 20 个自定义服务、独立内部别名与上游模型映射、双协议选择、增删改、独立凭据、原子注册表、Host 重载、重启恢复。
- 已覆盖：本地自定义 Responses 服务 + 真实 runtime 文件工具闭环；两协议最小请求路由；编辑密钥保护、运行中变更阻止、当前模型删除保护。
- 下一阶段：协议能力诊断与 Chat Completions 实际 Agent 专项适配；正式安装包及升级。此前「自定义服务编辑器待完成」状态由本条更新。

### macOS 安装预览增量

- 已实现：随应用分发固定 runtime、消除运行时对源码 cwd 的依赖、生成 Apple Silicon app/DMG、资源及镜像哈希、ad-hoc 签名校验。
- 回归：双模型开发版桌面流程；镜像安装副本、凭据重录与重启、真实 runtime 本地工具流程。
- 凭据升级实测要求用户重录，尚未实现跨身份透明迁移。
- 正式发行剩余：Developer ID 签名、公证、跨设备验收与独立身份迁移。Chat 扩展继续后置。

### 个人使用优先级与启动修复

- 按用户要求，发布签名、公证移出当前计划。
- 已补齐重复启动/激活恢复已有窗口，以及启动异常弹窗。
- 独立开发与安装副本启动回归纳入交付验证；保留单实例和任务不重放约束。

### 协议诊断与 Chat 专项增量

- 已实现：两协议分项诊断、失败/跳过状态、超时与脱敏、桌面入口。
- 已修复：Codex 默认可选 reasoning 输出字段阻断 Chat 文本 Agent 的 422；仍拒绝未支持推理控制。
- 新增真实 runtime Chat 文件工具、参数分片、历史重启、取消回归；真实供应商诊断与续接证据见 VERIFICATION.txt。
- 尚未完成：Chat 完整固定任务集与推理/多模态专项；个人安装版凭据 UI 稳定性继续单独跟踪。

## 2026-09-21：优先级调整与插件增量

Qwen 可靠性专项按用户决定暂缓，历史 16/20 不变。当前优先完成浏览器/MCP、模型配置与分组设置 UI。已引入固定版 Playwright MCP 浏览器工具及 stdio/Streamable HTTP 注册表；模型配置仅开放 OpenAI 兼容和 DeepSeek 两类，旧配置保持原行为；输入区和设置弹框升级。详见 [插件与设置增量](docs/PLUGINS_AND_SETTINGS_V1.md)。MCP 鉴权、浏览器可视会话、多模态工具输出与商业模型浏览任务耐久回归后续跟进。

## 2026-09-21：内嵌共享浏览器增量

- 已实现：Electron WebContentsView 单标签，Main 浏览器服务、HTTP MCP 适配、Host 任务绑定与单次审批；不依赖 Chrome。
- 已实现：导航/快照/点击/输入/后退/刷新/关闭，过期 ref 校验、接管/交还、任务停止/断连授权失效、临时 session 清理、弹窗隐藏原生 view。
- 已验证：本地确定性真实 Codex + Chat MCP 任务完整链路，以及打包应用；旧插件/Host/设置/审批/诊断回归。
- 下一步：逐资源 origin 授权、连接地址约束、真实站点兼容性、更完整 DOM/可访问性操作，然后持久 profile/多标签。当前跨站子资源默认阻止；不宣称完整网络沙箱。
- Qwen 可靠性仍暂缓，历史 16/20 不变；发布签名/公证仍不纳入个人使用目标。

## 2026-09-22：站点兼容性诊断

- 完成侧边栏阻止资源诊断、加载状态、origin 级脱敏、40 类上限及通知合并。
- 公开页面只读验证：Example Domain、Electron 文档、MDN 均加载并读取快照（3/3）；未认证登录/交易等交互。
- 下一步仍是逐 origin 资源授权与连接地址约束；本轮没有自动放开跨域资源。

## 2026-09-22：逐域资源授权

- 完成诊断列表资源授权/撤销、二次确认、临时会话生命周期、精确 origin/type 校验及本地组件/桌面回归。
- 本轮只开发版构建与测试，不更新安装包；后续安装验收再打包。
- 下一步：连接级地址约束、元素能力及真实站点交互任务集。授权暂不开放 XHR/iframe/WebSocket/导航。

## 浏览器任务级审批增量

- 增加一次性 / 本次任务同工具同站点 / 本次任务全部浏览器工具三种审批范围。
- Main 仍为每个请求签发单次精确参数授权；任务结束、停止、接管、断连清除。
- 本轮仅开发版构建和回归，不生成安装包。

## 命令任务级审批增量

- 完成相同命令及上下文的本次任务免重复审批，不扩展文件修改、终端输入或 MCP 权限。
- 真实 runtime 验证重复执行自动接受、不同命令再询问、新任务重新审批和拒绝无副作用。
- 未生成安装包。
